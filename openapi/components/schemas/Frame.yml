properties:
  id:
    anyOf:
      - nullable: true
      - description: The index of the window object corresponding to a frame.
        type: integer
        example: 0
      - description: An element id. The element must be the frame or iframe to be selected.
        type: string
        example: ELEMENT-1
type: object
required:
  - id
example:
  id: ELEMENT-1
