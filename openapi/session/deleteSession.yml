operationId: deleteSession
summary: Delete Session
externalDocs:
  description: See deleteSession command in WebDriver specification
  url: 'https://w3c.github.io/webdriver/#dfn-delete-session'
parameters:
  - $ref: '../main.yml#/components/parameters/sessionId'
responses:
  '200':
    description: empty response
    content:
      application/json:
        schema:
          $ref: '../main.yml#/components/schemas/NullResponse'
